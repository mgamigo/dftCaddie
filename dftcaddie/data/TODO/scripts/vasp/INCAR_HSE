SYSTEM = NONAME
PREC = Accurate         # precision
ENCUT = 500 
ISTART = 1              # restart with constant energy cutoff (orbitals read from WAVECAR)
ICHARG = 1              # read CHGCAR and self consistent charge density
EDIFF = 1.0e-05         # global break condition por the scf
LORBIT = 11             # the way to write PROCAR and PROOUT
#IALGO = 38             # algorithm used to optimized orbitals
ISMEAR = 0; SIGMA = 0.1 # gaussian smearing with its spread
LREAL = .FALSE.         # projector operators act in reciprocal space (default)
#ADDGRID = .TRUE.       # adds additional support grid to evaluate augmention changes
#LCHARG = .FALSE.       # whether CHGCAR and CHG are written
NPAR = 4                # number of bands treated in parallel (default=number of cores)
LSORBIT = TRUE          # whether spin orbit is taken into account
ISPIN = 1                # Allows for non-collinear spin
LWAVE = .TRUE.          # whether the wavefunctions are writen to the wavecar file

#GGA = PE
LHFCALC = .TRUE.        # specifies whether Hartree-Fock/DFT hybrid funcional calculation is done
HFSCREEN = 0.2          # the range separation parameter in range separated hybrid functionals (HSE06 is 0.2)
ALGO = All              # all band simultaneus update of orbitals
TIME = 0.4              # the time step for IALGO=5x (default is 0.4)
ENCUTFOCK = 0           # energy cutoff that determines the FFT grids used by the Hartree Fock routines (0 implies the smalles possible grid)
