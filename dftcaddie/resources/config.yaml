# ============================================================
# dftCaddie — User Configuration File
# ============================================================
#
# This file defines your calculation recipes and execution environment:
#   • Calculation kinds, flavors, settings, and templates
#   • Shared numerical defaults
#   • Clusters, MPI launch commands, and SBATCH headers
#   • Pseudopotential library locations
#
# Start by adapting a calculation definition, then configure the execution
# environment and pseudopotential sources. Run ``caddie config check
# --workflows`` after edits.
#
# User configuration directory:
#   ~/.config/dftcaddie/
# ============================================================


# ------------------------------------------------------------
# Calculation definitions
# ------------------------------------------------------------
# Each calculation kind defines:
#   name      → label displayed in the interactive client
#   config    → selectable settings; ``code`` is required
#   flavors   → complete workflow variants, when needed
#   files     → templates grouped by code
#
# ``code.options`` must match the keys under ``files`` exactly. Settings with a
# ``default`` are not prompted for unless ``caddie calc --details`` is used.
#
# Template paths are relative to ``templates/``. Include one ``master.sh`` in
# every code mapping. It is the submit-ready top-level script: caddie adds the
# selected SBATCH header and calls every other listed ``.sh`` file in order.
# ``master.sh`` never calls itself.
#
# A flavor has its own configuration and template mapping. For example, the
# two ``relax`` flavors below use different cell-relaxation settings.

calculations:
  bands:
    name: "Bands"
    config:
      - name: code
        prompt: "Select code:"
        options:
          - quantum_espresso
          - vasp
      - name: soc
        prompt: "Spin–orbit coupling:"
        options:
          - true
          - false
        default: true
    files:
      quantum_espresso:
        - quantum_espresso/scf.sh
        - quantum_espresso/bands.sh
        - quantum_espresso/project_bands.sh
        - quantum_espresso/SYSTEM.INFO
        - quantum_espresso/master.sh
      vasp:
        - vasp/INCAR.SCC
        - vasp/INCAR.BS
        - vasp/KPOINTS.SCC
        - vasp/scf.sh
        - vasp/bands.sh
        - vasp/master.sh
  relax:
    name: "Relax"
    flavors:
      fixed_cell:
        name: "Fixed cell"
        config:
          - name: code
            prompt: "Select code:"
            options:
              - quantum_espresso
              - vasp
          - name: soc
            prompt: "Spin–orbit coupling:"
            options:
              - true
              - false
            default: false
          - name: cell_relaxation
            prompt: "Cell relaxation?"
            options:
              - false
        files:
          quantum_espresso:
            - quantum_espresso/relax.sh
            - quantum_espresso/SYSTEM.INFO
            - quantum_espresso/master.sh
          vasp:
            - vasp/INCAR.RELAX
            - vasp/KPOINTS.SCC
            - vasp/relax.sh
            - vasp/master.sh
      variable_cell:
        name: "Variable cell"
        config:
          - name: code
            prompt: "Select code:"
            options:
              - quantum_espresso
              - vasp
          - name: soc
            prompt: "Spin–orbit coupling:"
            options:
              - true
              - false
            default: false
          - name: cell_relaxation
            prompt: "Cell relaxation?"
            options:
              - true
        files:
          quantum_espresso:
            - quantum_espresso/relax.sh
            - quantum_espresso/SYSTEM.INFO
            - quantum_espresso/master.sh
          vasp:
            - vasp/INCAR.RELAX
            - vasp/KPOINTS.SCC
            - vasp/relax.sh
            - vasp/master.sh
  phonons:
    name: "Phonons"
    config:
      - name: code
        prompt: "Select code:"
        options:
          - quantum_espresso
      - name: soc
        prompt: "Spin–orbit coupling:"
        options:
          - true
          - false
        default: false
    files:
      quantum_espresso:
        - quantum_espresso/scf.sh
        - quantum_espresso/ph.sh
        - quantum_espresso/matdyn.sh
        - quantum_espresso/SYSTEM.INFO
        - quantum_espresso/master.sh


# ------------------------------------------------------------
# Global numerical defaults
# ------------------------------------------------------------
# These numerical defaults are used by the system and pseudopotential helpers.
# They complement parameters stored in templates; they do not establish
# scientific convergence for a particular material.

# Target k-point density: kppra = number of k-points × number of atoms.
# Used by ``caddie set system --autokgrid`` and ``caddie calc --auto``.
default_kppra: 12000
# Multiplier for non-self-consistent grids relative to the self-consistent grid.
nscf_kppra_ratio: 4

# Safety factor for cutoffs read from pseudopotential headers.
# Final cutoff = suggested cutoff × default_cutoff_ratio.
default_cutoff_ratio: 1.5


# ------------------------------------------------------------
# Cluster definitions
# ------------------------------------------------------------
# Each cluster entry defines:
#   hostname      → substring used to detect the cluster
#   mpi_command   → launcher prepended to configured MPI executables
#   headers       → available SBATCH header presets
#
# Header files are resolved from:
#   ~/.config/dftcaddie/sbatch_headers/
#
# ``local`` is the fallback when no hostname matches. During ``caddie calc``,
# the first header listed for the detected cluster is added to ``master.sh``.

clusters:
  local:
    hostname: ""
    mpi_command: "mpiexec -np 2"
    headers:
      - name: default
        file: local.default
  cluster1:
    hostname: "cluster1_hostname"
    mpi_command: "mpiexec -np $NPROCS"
    headers:
      - name: default
        file: cluster1.default
  cluster2:
    hostname: "cluster2_hostname"
    mpi_command: "srun --mpi=pmi2"
    headers:
      - name: default
        file: cluster2.default
      - name: long
        file: cluster2.long
      - name: small
        file: cluster2.small


# ------------------------------------------------------------
# MPI-aware executables
# ------------------------------------------------------------
# When one of these executables occurs in a generated shell script, caddie
# prepends the selected cluster's ``mpi_command``. Add executables used in your
# own templates here.

mpi_executables:
  - pw.x
  - projwfc.x
  - ph.x
  - fs.x
  - pw2wannier90.x
  - wannier90.x
  - vasp_ncl


# ------------------------------------------------------------
# Pseudopotential locations
# ------------------------------------------------------------
# Uncomment and set either entry when you use automatic pseudopotential setup.
# ``~`` is expanded. QE checks $PSLIBRARY first; it overrides qe_pslibrary.
# VASP expects the root directory containing its exchange/kind subdirectories.

#qe_pslibrary: /path/to/pslibrary
#vasp_pseudopotentials: /path/to/vasp/potentials

# ------------------------------------------------------------
# Suggested Quantum ESPRESSO pseudopotentials
# ------------------------------------------------------------
# Wildcard patterns used to select default QE pseudopotentials from PSLibrary.
#
# Special placeholders:
#   $fct → exchange functional (pbe, pbesol, pz, ...)
#   *    → pseudo kind (kjpaw, us, ...)
#
# Caddie uses the element's pattern to choose one preferred pseudo. Customize
# an entry when your PSLibrary version or preferred potential differs.

suggested_qe_pseudos:
  H: "H.$fct-*_psl.1.0.0"
  He: "He.$fct-*_psl.1.0.0"
  Li: "Li.$fct-sl-*_psl.1.0.0"
  Be: "Be.$fct-sl-*_psl.1.0.0"
  B: "B.$fct-n-*_psl.1.0.0"
  C: "C.$fct-n-*_psl.1.0.0"
  N: "N.$fct-n-*_psl.1.0.0"
  O: "O.$fct-n-*_psl.1.0.0"
  F: "F.$fct-n-*_psl.1.0.0"
  Ne: "Ne.$fct-n-*_psl.1.0.0"
  Na: "Na.$fct-spnl-*_psl.1.0.0"
  Mg: "Mg.$fct-spnl-*_psl.1.0.0"
  Al: "Al.$fct-nl-*_psl.1.0.0"
  Si: "Si.$fct-nl-*_psl.1.0.0"
  P: "P.$fct-nl-*_psl.1.0.0"
  S: "S.$fct-nl-*_psl.1.0.0"
  Cl: "Cl.$fct-nl-*_psl.1.0.0"
  Ar: "Ar.$fct-nl-*_psl.1.0.0"
  K: "K.$fct-spn-*_psl.1.0.0"
  Ca: "Ca.$fct-spn-*_psl.1.0.0"
  Sc: "Sc.$fct-spn-*_psl.1.0.0"
  Ti: "Ti.$fct-spn-*_psl.1.0.0"
  V: "V.$fct-spnl-*_psl.1.0.0"
  Cr: "Cr.$fct-spn-*_psl.1.0.0"
  Mn: "Mn.$fct-spn-*_psl.0.3.1"
  Fe: "Fe.$fct-n-*_psl.1.0.0"
  Co: "Co.$fct-n-*_psl.0.3.1"
  Ni: "Ni.$fct-n-*_psl.1.0.0"
  Cu: "Cu.$fct-dn-*_psl.1.0.0"
  Zn: "Zn.$fct-dn-*_psl.1.0.0"
  Ga: "Ga.$fct-dnl-*_psl.1.0.0"
  Ge: "Ge.$fct-n-*_psl.1.0.0"
  As: "As.$fct-n-*_psl.1.0.0"
  Se: "Se.$fct-n-*_psl.1.0.0"
  Br: "Br.$fct-n-*_psl.1.0.0"
  Kr: "Kr.$fct-dn-*_psl.1.0.0"
  Rb: "Rb.$fct-spn-*_psl.1.0.0"
  Sr: "Sr.$fct-spn-*_psl.1.0.0"
  Y: "Y.$fct-spn-*_psl.1.0.0"
  Zr: "Zr.$fct-spn-*_psl.1.0.0"
  Nb: "Nb.$fct-spn-*_psl.1.0.0"
  Mo: "Mo.$fct-spn-*_psl.1.0.0"
  Tc: "Tc.$fct-spn-*_psl.0.3.0"
  Ru: "Ru.$fct-spn-*_psl.1.0.0"
  Rh: "Rh.$fct-spn-*_psl.1.0.0"
  Pd: "Pd.$fct-n-*_psl.1.0.0"
  Ag: "Ag.$fct-n-*_psl.1.0.0"
  Cd: "Cd.$fct-n-*_psl.1.0.0"
  In: "In.$fct-dn-*_psl.1.0.0"
  Sn: "Sn.$fct-dn-*_psl.1.0.0"
  Sb: "Sb.$fct-n-*_psl.1.0.0"
  Te: "Te.$fct-n-*_psl.1.0.0"
  I: "I.$fct-n-*_psl.1.0.0"
  Xe: "Xe.$fct-dn-*_psl.1.0.0"
  Cs: "Cs.$fct-spnl-*_psl.1.0.0"
  Ba: "Ba.$fct-spn-*_psl.1.0.0"
  La: "La.$fct-spfn-*_psl.1.0.0"
  Ce: "Ce.$fct-spdn-*_psl.1.0.0"
  Pr: "Pr.$fct-spdn-*_psl.1.0.0"
  Nd: "Nd.$fct-spdn-*_psl.1.0.0"
  Pm: "Pm.$fct-spdn-*_psl.1.0.0"
  Sm: "Sm.$fct-spdn-*_psl.1.0.0"
  Eu: "Eu.$fct-spn-*_psl.1.0.0"
  Gd: "Gd.$fct-spdn-*_psl.1.0.0"
  Tb: "Tb.$fct-spdn-*_psl.1.0.0"
  Dy: "Dy.$fct-spdn-*_psl.1.0.0"
  Ho: "Ho.$fct-spdn-*_psl.1.0.0"
  Er: "Er.$fct-spdn-*_psl.1.0.0"
  Tm: "Tm.$fct-spdn-*_psl.1.0.0"
  Yb: "Yb.$fct-spn-*_psl.1.0.0"
  Lu: "Lu.$fct-spdn-*_psl.1.0.0"
  Hf: "Hf.$fct-spn-*_psl.1.0.0"
  Ta: "Ta.$fct-spn-*_psl.1.0.0"
  W: "W.$fct-spn-*_psl.1.0.1"
  Re: "Re.$fct-spn-*_psl.1.0.0"
  Os: "Os.$fct-spn-*_psl.1.0.0"
  Ir: "Ir.$fct-n-*_psl.0.2.3"
  Pt: "Pt.$fct-n-*_psl.1.0.0"
  Au: "Au.$fct-n-*_psl.1.0.1"
  Hg: "Hg.$fct-n-*_psl.1.0.0"
  Tl: "Tl.$fct-dn-*_psl.1.0.0"
  Pb: "Pb.$fct-dn-*_psl.1.0.0"
  Bi: "Bi.$fct-dn-*_psl.1.0.0"
  Po: "Po.$fct-dn-*_psl.1.0.0"
  At: "At.$fct-dn-*_psl.1.0.0"
  Rn: "Rn.$fct-dn-*_psl.1.0.0"
  Fr: "Fr.$fct-spdn-*_psl.1.0.0"
  Ra: "Ra.$fct-spdn-*_psl.1.0.0"
  Ac: "Ac.$fct-spfn-*_psl.1.0.0"
  Th: "Th.$fct-spfn-*_psl.1.0.0"
  Pa: "Pa.$fct-spfn-*_psl.1.0.0"
  U: "U.$fct-spfn-*_psl.1.0.0"
  Np: "Np.$fct-spfn-*_psl.1.0.0"
  Pu: "Pu.$fct-spfn-*_psl.1.0.0"
